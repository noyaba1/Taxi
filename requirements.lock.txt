# Fully-resolved dependency set -- the TRANSITIVE closure, not just the direct
# pins in requirements.txt. Generated by `pip freeze` from a venv built from
# scratch on 2026-08-02 (Python 3.11.10, macOS/arm64) that ran the full test
# suite green: 60 passed.
#
# requirements.txt says what this project asks for and why; this file says what
# it actually resolved to. Install with:
#
#     python3.11 -m venv .venv
#     .venv/bin/pip install -r requirements.lock.txt
#
# NOT captured here, because pip cannot: a JDK 8/11/17 (see SETUP.md). Every
# Python dep can be green and Spark still dies in the JVM -- `validate_env`
# checks both, and is the real gate.
#
# Platform note: pyspark/pyarrow/numpy resolve to arm64 wheels here. On a
# different platform re-freeze rather than forcing these exact artefacts.
Jinja2==3.1.6
MarkupSafe==3.0.3
branca==0.8.2
certifi==2026.7.22
charset-normalizer==3.4.9
contourpy==1.3.3
cycler==0.12.1
datasketches==5.0.2
folium==0.16.0
fonttools==4.63.0
h3==3.7.7
idna==3.18
iniconfig==2.3.0
kiwisolver==1.5.0
matplotlib==3.9.0
numpy==1.26.4
packaging==26.2
pandas==2.2.2
pillow==12.3.0
pluggy==1.6.0
py4j==0.10.9.7
pyarrow==15.0.2
pyparsing==3.3.2
pyspark==3.5.1
pytest==8.2.2
python-dateutil==2.9.0.post0
python-geohash==0.8.5
pytz==2026.3.post1
requests==2.34.2
six==1.17.0
tqdm==4.66.4
tzdata==2026.3
urllib3==2.7.0
xyzservices==2026.3.0
